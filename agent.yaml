spec_version: v1

name: Guidr AI Agent
description: AI agent for planning, resume improvement, and growth tracking using Git memory.

model:
  provider: openai
  name: gpt-4o-mini

memory:
  type: git
  auto_commit: true

skills:
  - daily_planner

entrypoint: main.py

gitclaw:
  enabled: true
  auto_commit: true
  execution_mode: autonomous